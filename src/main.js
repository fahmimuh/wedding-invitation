/**
 * Vite entry point.
 *
 * The invitation currently keeps its carefully tuned visual markup and
 * animation script in index.html. This module is intentionally small so the
 * next pass can move envelope and invitation behavior into separate modules
 * without changing the page structure again.
 */
document.documentElement.dataset.app = 'wedding-invitation';
